<nav>
  <span class="float_l"><%= link_to "Home", root_path %></span>
  <% if !user_signed_in? %>
    <span><%= link_to "Sign Up", new_user_registration_path %></span>
    <span><%= link_to "Sign In", user_session_path %></span>
  <% end %>
  <% if user_signed_in? %>
      <span><%= link_to "Meetings", posts_path%></span>
      <span><%= link_to "Account", edit_user_registration_path%></span>
      <span><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></span>
  <% end %>
</nav>
